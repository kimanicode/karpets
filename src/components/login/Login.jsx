import React from 'react'

const Login = () => {
  return (
    <div className='bg-slate-900'>
        <h2 >Here is the login pageAnother issue is that the exact prop is unnecessary if you only have one route defined. The exact prop is typically used to match the exact path of a route when there are multiple routes defined.</h2>

    </div>
  )
}

export default Login